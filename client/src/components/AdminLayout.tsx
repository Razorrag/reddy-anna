
const AdminLayout = ({ children }: { children: React.ReactNode }) => {
  // Simplified layout: remove legacy sidebar/header to match unified purple UI
  return <>{children}</>;
};

export default AdminLayout;
