# 🔐 SUPABASE CONFIGURATION (REPLACE WITH YOUR ACTUAL VALUES)
# Get these from your Supabase Dashboard > Settings > API
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY_HERE
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_SERVICE_KEY=YOUR_SUPABASE_SERVICE_KEY_HERE

# 🎯 JWT CONFIGURATION (GENERATE SECURE SECRETS)
# Use: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-key-min-32-characters-long
JWT_EXPIRES_IN=24h
JWT_ISSUER=AndarBaharApp
JWT_AUDIENCE=users

# 🖥️ SERVER CONFIGURATION
NODE_ENV=development
PORT=5000
WEBSOCKET_URL=ws://localhost:5000

# 🌐 CORS CONFIGURATION
CORS_ORIGIN=http://localhost:3000

# 📡 RTMP SERVER CONFIGURATION (for live streaming)
RTMP_SERVER_PORT=1936
RTMP_HTTP_PORT=8001
RTMP_APP_NAME=live

# 🗄️ DATABASE CONFIGURATION (if using PostgreSQL directly)
DATABASE_URL=postgresql://username:password@localhost:5432/andar_bahar

# 🌍 CLIENT-SIDE API CONFIGURATION
VITE_API_BASE_URL=localhost:5000

# 🔒 SESSION CONFIGURATION
SESSION_SECRET=new-secure-secret-change-in-production-IMPORTANT

# 📊 ADDITIONAL CONFIGURATION
MIN_BET_AMOUNT=1000
MAX_BET_AMOUNT=50000
DEFAULT_BALANCE=10000
HOUSE_COMMISSION=0.05

# 🚀 PRODUCTION CONFIGURATION (uncomment for production)
# NODE_ENV=production
# CORS_ORIGIN=https://yourdomain.com
# VITE_API_BASE_URL=api.yourdomain.com
# WEBSOCKET_URL=wss://api.yourdomain.com

# 📝 NOTES:
# 1. Replace YOUR_PROJECT_ID with your actual Supabase project ID
# 2. Replace YOUR_SUPABASE_ANON_KEY_HERE with your Supabase anon key
# 3. Replace YOUR_SUPABASE_SERVICE_KEY_HERE with your Supabase service role key
# 4. Generate secure JWT secrets using: openssl rand -base64 32
# 5. Never commit real secrets to version control
# 6. Copy this file to .env and fill in your actual values
