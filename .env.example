# 🔐 SUPABASE CONFIGURATION (REQUIRED - REPLACE WITH YOUR ACTUAL VALUES)
# Get these from your Supabase Dashboard > Settings > API
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY_HERE
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_SERVICE_KEY=YOUR_SUPABASE_SERVICE_KEY_HERE

# 🎯 JWT CONFIGURATION (REQUIRED - GENERATE SECURE SECRET)
# Generate with: openssl rand -base64 32
# This is CRITICAL for authentication - keep it secret!
JWT_SECRET=your-secure-jwt-secret-key-min-32-characters-long
JWT_EXPIRES_IN=24h

# 🖥️ SERVER CONFIGURATION
NODE_ENV=development
PORT=5000
WEBSOCKET_URL=ws://localhost:5000

# 🌐 CORS CONFIGURATION
# For development, use localhost. For production, use your actual domain
# Multiple origins can be comma-separated
CORS_ORIGIN=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://yourdomain.com

# 🗄️ DATABASE CONFIGURATION (if using PostgreSQL directly)
DATABASE_URL=postgresql://username:password@localhost:5432/andar_bahar

# 🌍 CLIENT-SIDE API CONFIGURATION
# For development: localhost:5000
# For production: your-api-domain.com (without protocol)
VITE_API_BASE_URL=localhost:5000
VITE_WS_URL=ws://localhost:5000/ws
# In production, these will be dynamically set based on window.location

# 🔒 SESSION CONFIGURATION - NOT NEEDED (Using JWT-only auth)
# SESSION_SECRET is no longer used - authentication is now JWT-only

# 📊 GAME CONFIGURATION
MIN_BET=1000
MAX_BET=100000
DEFAULT_BALANCE=0.00
DEFAULT_TIMER_DURATION=30
HOUSE_COMMISSION=0.05

# 🔒 RATE LIMITING CONFIGURATION
MAX_BETS_PER_MINUTE=30
RATE_LIMIT_WINDOW_MS=60000

# 💰 PAYMENT CONFIGURATION
MIN_DEPOSIT=100
MAX_DEPOSIT=1000000
MIN_WITHDRAWAL=500
MAX_WITHDRAWAL=500000

# 🔴 REDIS CONFIGURATION - NOT NEEDED (JWT is stateless)
# Redis is not required for JWT authentication (stateless)
# Only needed if you want to cache game state or other data
# REDIS_URL=
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# 🚀 PRODUCTION CONFIGURATION (uncomment for production)
# NODE_ENV=production
# CORS_ORIGIN=https://yourdomain.com
# VITE_API_BASE_URL=api.yourdomain.com
# WEBSOCKET_URL=wss://api.yourdomain.com
# REDIS_URL=redis://your-redis-host:6379

# 📝 NOTES:
# 1. Replace YOUR_PROJECT_ID with your actual Supabase project ID
# 2. Replace YOUR_SUPABASE_ANON_KEY_HERE with your Supabase anon key
# 3. Replace YOUR_SUPABASE_SERVICE_KEY_HERE with your Supabase service role key
# 4. Generate secure JWT_SECRET using: openssl rand -base64 32
# 5. Never commit real secrets to version control
# 6. Copy this file to .env and fill in your actual values
# 7. JWT_SECRET is REQUIRED - server will not start without it
# 8. SESSION_SECRET is NOT needed - we use JWT-only authentication
