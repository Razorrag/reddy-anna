# Add these lines to your .env file

# FFmpeg path for RTMP to HLS transcoding
# Windows (if installed via chocolatey or manual install):
FFMPEG_PATH=C:/ffmpeg/bin/ffmpeg.exe
# Or if in PATH:
# FFMPEG_PATH=ffmpeg

# macOS (installed via homebrew):
# FFMPEG_PATH=/opt/homebrew/bin/ffmpeg
# Or:
# FFMPEG_PATH=/usr/local/bin/ffmpeg

# Linux/Render:
# FFMPEG_PATH=/usr/bin/ffmpeg

# For development, if ffmpeg is in your PATH, you can use:
FFMPEG_PATH=ffmpeg
